// Generated by CoffeeScript 1.3.3
(function() {
  var handleNavClick, handleSecondaryNavClick, log;

  log = function(str) {
    return console.log(str);
  };

  handleNavClick = function(evt) {
    var clicked, groupId;
    clicked = evt.target;
    $('nav.primary .selected').removeClass('selected');
    $(evt.target.parentNode).addClass('selected');
    groupId = $(clicked).attr('data-group-id');
    $('.products').addClass('hidden');
    setTimeout(function() {
      $('.products > a').each(function(i, productLink) {
        var isInGroup;
        isInGroup = $(productLink).hasClass('group' + groupId);
        if (isInGroup) {
          return $(productLink).removeClass('hidden');
        } else {
          return $(productLink).addClass('hidden');
        }
      });
      return $('.products').removeClass('hidden');
    }, 500);
    if ($('.page_container').hasClass('home') || $(clicked.parentNode).hasClass('logo')) {
      return $('.page_container').toggleClass('home');
    }
  };

  handleSecondaryNavClick = function(evt) {
    return console.log('happened');
  };

  $(function() {
    $('.primary > div > a').each(function(i, navItem) {
      return $(navItem).on('click', handleNavClick);
    });
    $('.secondary > a').each(function(i, secondaryNavItem) {
      return $(secondaryNavItem).on('click', handleSecondaryNavClick);
    });
    return $('.secondary').each(function(i, secondaryNav) {
      return secondaryNav.style.left = ((i + 1) * 160) + 'px';
    });
  });

}).call(this);
